language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
cache: cargo
before_script:
  - sudo apt-get update
script:
  - cargo build --verbose --all
  - SERVER=127.0.0.1:3000
  - cargo test --verbose --all
